name: Test Approval Workflow

on:
  pull_request_review:
    types: [submitted]

jobs:
  test-approval:
    if: >
      github.event.review.state == 'approved' &&
      github.event.review.user.id == 238416997
    runs-on: ubuntu-latest

    steps:
      - name: Print success message
        run: |
          echo "🎉 Workflow triggered!"
          echo "Approved by correct user: ${{ github.event.review.user.login }}"
